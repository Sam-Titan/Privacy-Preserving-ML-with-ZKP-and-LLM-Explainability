# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.1.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
joblib>=1.3.0

# ── ONNX export ───────────────────────────────────────────────────────────────
onnx>=1.14.0
onnxruntime>=1.16.0

# ── Zero-Knowledge Proofs ─────────────────────────────────────────────────────
# EZKL handles ONNX → ZK circuit compilation, proof generation & verification.
# Requires Rust toolchain installed on the system.
ezkl>=10.0.0`

# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# ── LLM (Ollama client) ───────────────────────────────────────────────────────
requests>=2.31.0

# ── Optional: notebook / evaluation ──────────────────────────────────────────
# jupyter>=1.0.0
# matplotlib>=3.7.0